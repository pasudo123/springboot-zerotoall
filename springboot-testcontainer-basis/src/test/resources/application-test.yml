spring:
  datasource:
    hikari:
      auto-commit: true

  jpa:
    open-in-view: true

    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate.format_sql: true

server:
  port: 48076
