spring:
  datasource:
    hikari:
      auto-commit: true

  jpa:
    open-in-view: false

  main:
    allow-bean-definition-overriding: true

logging:
  level:
    org.hibernate.type.descriptor.sql: TRACE